const firstName = "Bruce";
const lastName = "Wayne";

const fullName = firstName + " " + lastName;
const fullNameES6 = `${firstName} ${lastName}`;

const milista = `
<ul>
  <li>${firstName}</li>
  <li>${lastName}</li>
</ul>
`;
